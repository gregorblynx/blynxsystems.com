{
  "name": "blynx-aima-agency-site",
  "version": "1.0.0",
  "private": true,
  "description": "Official static website for BLYNX AIMA AGENCY.",
  "scripts": {
    "generate": "node scripts/generate-pages.js",
    "build": "node scripts/generate-pages.js && node scripts/build.js",
    "serve": "python3 -m http.server 4173"
  }
}
